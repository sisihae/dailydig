# Daily Music Discovery Agent

## Project Specs

All specifications live in `docs/specs/`. Start with `docs/specs/README.md` for the full index.
Read the relevant spec file before implementing any module.

## Implementation Plans

All implementation plans live in `docs/plans/`. Start with `docs/plans/README.md` for the full index.
Read the relevant plan doc before implementing any module.

## Workflow

1. Read the spec for the module you're about to build (`docs/specs/`).
2. Read the corresponding plan doc (`docs/plans/`).

Ask the user if something is vague or ambiguous. Make everything clear and planned before starting implementation.
